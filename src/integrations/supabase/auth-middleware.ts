// Stub: this project is a static SPA on Vercel. Server-only middleware is not used.
// Kept as a no-op so any stale imports do not break the build.
export const requireSupabaseAuth = undefined as never;
